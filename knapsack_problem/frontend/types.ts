/**
 * Knapsack panel config types.
 * The knapsack problem uses only the base solver fields — no problem-specific extensions.
 * Re-exported here so knapsack-specific code has a local canonical import path.
 */

export type { BaseProblemBlock as ProblemBlock } from "@problemConfig/types";
